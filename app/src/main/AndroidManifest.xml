<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hu.dushu.developers.sunshine">

    <!-- <uses-sdk android:minSdkVersion="11" /> -->

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DetailActivity"
            android:label="@string/title_activity_detail"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="hu.dushu.developers.sunshine.MainActivity" />
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="hu.dushu.developers.sunshine.MainActivity" />
        </activity>

        <provider
            android:name=".data.WeatherProvider"
            android:authorities="hu.dushu.developers.sunshine"
            android:exported="true" />

    </application>

    <permission
        android:name="hu.dushu.developers.sunshine.LICENCE_TO_KILL"
        android:label="Licence to Kill"
        android:protectionLevel="dangerous" />

</manifest>
